const fs = require("fs")

module.exports = class Blacklist {
    static getBlacklist (id) {
        
    }

    static addBlacklist (id) {
        
    }

    static removeBlacklist (id) {
        
    }
}